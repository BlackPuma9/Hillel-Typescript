# Hillel Typescript

### Step 1 — Package.json

**npm start**

### Step 2 — Package.json

**npm start-server**

## HW7. ReverseArray

The function takes an array as an argument and reverses it Array.prototype.reverse

IMPORTANT

The function should change the input array, not create a new one 

`const arr = [1, 2, 3]
`

`const reversedArr = reverseArray(arr)
`

`reversedArr === arr // true`